# =============================================================================
# TinyMem0 环境变量配置示例
# =============================================================================
# 说明：将此文件复制为 .env 并根据需要修改

# -----------------------------------------------------------------------------
# 固定配置（不建议修改）
# -----------------------------------------------------------------------------
# HuggingFace镜像地址（加速国内下载）
HF_ENDPOINT=https://hf-mirror.com

# -----------------------------------------------------------------------------
# 阿里云API配置（使用云端API时需要）
# -----------------------------------------------------------------------------
# 阿里云DashScope API密钥
# 获取地址: https://dashscope.console.aliyun.com/apiKey
DASHSCOPE_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# 模型配置
# -----------------------------------------------------------------------------

# 是否使用本地LLM（true=本地模型, false=云端API）
USE_LOCAL_LLM=true

# 本地LLM模型选择（仅当USE_LOCAL_LLM=true时有效）
# 可选值: qwen2.5-7b, qwen2.5-3b, qwen2.5-1.5b, mistral-7b, llama3-8b, yi-6b
MODEL_SHORTCUT=mistral-7b

# 模型格式（仅当USE_LOCAL_LLM=true时有效）
# 可选值: gguf（推荐，CPU友好）, safetensors（需要GPU）
MODEL_FORMAT=gguf

# GGUF量化精度（仅当MODEL_FORMAT=gguf时有效）
# 可选值: Q3_K_M（最小）, Q4_K_M（推荐）, Q5_K_M（平衡）, Q8_0（最高质量）
MODEL_QUANTIZATION=Q4_K_M

# 本地模型路径（自动下载后会填充，也可手动指定完整路径）
LOCAL_MODEL_PATH=

# 嵌入模型配置
# HuggingFace模型ID，如 BAAI/bge-small-zh-v1.5
LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 嵌入向量维度（留空则自动设置：本地512，云端1536）
EMBEDDING_DIM=

# 是否跳过模型下载（true=直接使用已下载的模型）
SKIP_DOWNLOAD=false

# -----------------------------------------------------------------------------
# 评测配置
# -----------------------------------------------------------------------------
# MAX_SAMPLES=100
# VERBOSE_EVAL=false
