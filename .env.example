# =============================================================================
# TinyMem0 环境变量配置
# =============================================================================
# 说明：将此文件复制为 .env 并根据需要修改

# -----------------------------------------------------------------------------
# 固定配置（不建议修改）
# -----------------------------------------------------------------------------
# HuggingFace镜像地址（加速国内下载）
HF_ENDPOINT=https://hf-mirror.com

# -----------------------------------------------------------------------------
# 阿里云API配置（使用云端API时需要）
# -----------------------------------------------------------------------------
# 阿里云DashScope API密钥
# 获取地址: https://dashscope.console.aliyun.com/apiKey
# DASHSCOPE_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# 可选配置（优先级低于命令行参数）
# -----------------------------------------------------------------------------
# 以下配置可通过命令行参数覆盖，建议使用命令行参数而非修改此文件

# 使用本地LLM还是云端API（命令行: --use-local / --use-cloud）
# USE_LOCAL_LLM=true

# 本地模型路径（命令行: --model + --format + --quant）
# LOCAL_MODEL_PATH=models/gguf/mistral-7b-instruct-v0.2.Q4_K_M.gguf

# 本地嵌入模型（命令行: --embedding-model）
# LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 嵌入向量维度（命令行: --embedding-dim）
# EMBEDDING_DIM=512

# -----------------------------------------------------------------------------
# 评测配置
# -----------------------------------------------------------------------------
# MAX_SAMPLES=100
# VERBOSE_EVAL=false

